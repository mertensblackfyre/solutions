#include <iostream>
#include <vector>

class Solution {
public:
  std::vector<int> replaceElements(std::vector<int> &arr) {

    int p1 = 0;
    int p2 = p1 + 1;

    while (p1 < arr.size()) {

        if(wh) {
        
        }

         }

    for (int num : arr) {
      std::cout << num << std::endl;
    }

    return arr;

  }
};

int main() {

  Solution m;

  std::vector<int> arr = {17, 18, 5, 4, 6, 1};

  m.replaceElements(arr);

  return 0;
}
