#include <iostream>
#include <vector>

class Solution {
public:
    int minimumDifference(std::vector<int>& nums, int k) {
        
    }
};
int main() {

  Solution x;
  std::vector<int> nums = {9,4,1,7};
  x.minimumDifference(nums, 2);
}
